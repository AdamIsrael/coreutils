[workspace]
members = [
    "arch",
    "b2sum",
    "base32",
    "base64",
    "cat",
    "chown",
    "basename",
    "dirname",
    "echo",
    "env",
    "wc",
]

[workspace.dependencies]
clap = { version = "4.0", features = ["derive"] }
